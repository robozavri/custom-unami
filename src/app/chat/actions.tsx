'use server';

// import React from 'react';

// export async function streamMessage(formData: FormData) {
//   const prompt = String(formData.get('prompt') ?? '').trim() || 'Say hello!';

//   // This function is no longer used - chat goes through /api/chat route
//   return (
//     <div style={{ color: '#6b7280', background: '#f3f4f6', padding: 8, borderRadius: 6 }}>
//       Chat functionality moved to API route. Use the chat interface above.
//     </div>
//   );
// }
